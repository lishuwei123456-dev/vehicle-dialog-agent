services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_ENV: demo
      STATE_BACKEND: redis
      REDIS_URL: redis://redis:6379/0
      WEATHER_PROVIDER: demo
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
